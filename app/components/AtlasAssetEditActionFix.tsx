"use client";

// Legacy compatibility mount. The active asset Edit behavior is handled by
// AtlasAssetHeaderEditFix so there is only one click path for the header Edit button.
export default function AtlasAssetEditActionFix() {
  return null;
}
